[package]
name = "logos-ir"
version = "0.1.0"
edition = "2024"
description = "Logos internal IR and Calcite JSON importer."
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
